Keeping track of what I installed to later convert to cnv file

conda install pytorch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 pytorch-cuda=11.8 -c pytorch -c nvidia
pip uninstall -y numpy || true
conda install -y numpy=1.26.*  # (1.26.4 is fine)
conda config --env --add pinned_packages "numpy<2"
conda install -y "numpy<2"  # e.g., 1.26.4
conda install -y -c conda-forge ittnotify  ||  conda install -y -c conda-forge ittapi
conda install -y -c conda-forge intel-openmp

## problem was actually mkl versino <=2024 remember that shrey